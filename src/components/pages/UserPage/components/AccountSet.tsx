import AvatarCarousel from "@/components/pages/Authentication/components/AvatarCarousel";

export default function AccountSet() {
  return (
    <>
      <AvatarCarousel onSelect={() => {}} selectedAvatar={null} />
    </>
  );
}
